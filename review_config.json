{
  "upload_directory": "/tmp/files/",
  "output_directory": "/tmp/outputs/",
  "system_prompt": "You are an expert code reviewer. Analyze the provided codebase for bugs, security issues, and correctness problems. Provide actionable feedback. All files are located in /tmp/files",
  "task_prompt": "You are an expert code reviewer. Your task is to analyze the jmxterm codebase. jmxterm is a command-line JMX client that connects to MBean servers, runs commands (open, close, get, set, run, beans, domain, etc.), and handles file/stream I/O, session state, and JDK-specific process discovery. The codebase handles command parsing and chaining, JMX connection lifecycle, MBean attribute and operation invocation, notification subscription, and URL resolution (host:port and PID). Your goal is to identify bugs, correctness issues, boundary condition errors, logic errors, off-by-one mistakes, missing validation checks, resource leaks, and any other issues that could affect reliability or correctness. Focus on: control flow in command chaining and short-circuit behavior, connection and session lifecycle (connect/disconnect/close), null checks and guards before API calls, use of getConnection() vs isConnected() when checking session state, executor and scheduler lifecycle in watch/report mode, exception handling in close() and flush(), and regex patterns for URL/host parsing. Write your findings to /tmp/outputs/review.md",
  "rubric": [
    {
      "criterion": "Identifies that in CommandCenter.doExecute(String command) in src/main/java/org/cyclopsgroup/jmxterm/cc/CommandCenter.java the loop over &&-delimited command segments does not short-circuit: all segments are executed even when execute(c) returns false, so a failing command in the chain does not prevent later commands from running",
      "weight": 1
    },
    {
      "criterion": "Identifies that in SessionImpl.disconnect() in src/main/java/org/cyclopsgroup/jmxterm/cc/SessionImpl.java the JMX connector is never closed: connection is set to null without calling connection.close() first, causing a resource leak when disconnecting",
      "weight": 1
    },
    {
      "criterion": "Identifies that in OpenCommand.execute() in src/main/java/org/cyclopsgroup/jmxterm/cmd/OpenCommand.java when url is null the code calls session.getConnection() without first checking session.isConnected(), so when not connected it throws IllegalStateException instead of printing 'not connected' (linkage: SessionImpl.getConnection() throws when connection is null)",
      "weight": 1
    },
    {
      "criterion": "Identifies the missing null check in GetCommand.displayAttributes() in src/main/java/org/cyclopsgroup/jmxterm/cmd/GetCommand.java: after BeanCommand.getBeanName(bean, domain, session) the code calls new ObjectName(beanName) without guarding against null beanName, which can throw when bean resolves to null (e.g. isNull(bean))",
      "weight": 1
    },
    {
      "criterion": "Identifies that in DomainCommand.getDomainName(String domain, Session session) in src/main/java/org/cyclopsgroup/jmxterm/cmd/DomainCommand.java the connection check uses session.getConnection() != null instead of session.isConnected(), so when the session is not connected getConnection() throws before the Validate message is used (linkage: SessionImpl.getConnection() throws when connection is null)",
      "weight": 1
    },
    {
      "criterion": "Identifies that in WatchCommand.execute() in src/main/java/org/cyclopsgroup/jmxterm/cmd/WatchCommand.java when report is true and stopAfter is greater than 0 the method returns without awaiting the scheduled executor's termination, so the executor may still be running after execute() returns and the next command can run concurrently",
      "weight": 1
    },
    {
      "criterion": "Identifies that in FileCommandOutput.close() in src/main/java/org/cyclopsgroup/jmxterm/io/FileCommandOutput.java fileWriter.flush() is called followed by fileWriter.close() without try/finally, so if flush() throws an exception close() is never called and the file writer is left open",
      "weight": 1
    },
    {
      "criterion": "Identifies that in SyntaxUtils in src/main/java/org/cyclopsgroup/jmxterm/SyntaxUtils.java the PATTERN_HOST_PORT regex used by getUrl(String url, JavaProcessManager jpm) does not allow a hyphen in the host part, so host:port strings like my-host:9999 do not match and are incorrectly passed to JMXServiceURL as full URLs",
      "weight": 1
    }
  ]
}
